<div id="p-user-listing">
  <p>
    Link your accounts to unlock special features. None of your <%=
    Danbooru.config.app_name %> data will be shared with other sites.
  </p>

  <% LinkedAccount::SITES.each do |site| %>
    <%= render "linked_account", linked_account: user.linked_accounts.find_or_initialize_by(type: "LinkedAccount::#{site}") %>
  <% end %>
</div>
